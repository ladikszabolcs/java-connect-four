package org.connectfour.controller;

import org.connectfour.model.Model;
import org.connectfour.view.View;

public class Controller {
    private Model model;
    private View view;

    public Controller(Model model, View view) {
        this.model = model;
        this.view = view;

        // Controller logic to handle interactions will be added later
    }

    // You will add methods here to handle user inputs and update the model/view
}